<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>genres</title>
  </head>
  <body>
    <h1>THESE ARE ALL THE genreS</h1>

    <% Genre.all.each do |genre| %>
      <%= genre.id %>
      <%= genre.name %>
      <a href=<%="/genres/#{genre.slug}"%>>linkety</a>
      <%end%>

  </body>
</html>
